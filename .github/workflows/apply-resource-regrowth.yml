name: Apply Resource Regrowth

on:
  push:
    branches:
      - work/resource-regrowth
    paths:
      - tools/apply-resource-regrowth.py
      - .github/workflows/apply-resource-regrowth.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/resource-regrowth
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply deterministic resource regrowth
        run: python tools/apply-resource-regrowth.py
      - name: Commit integration
        run: |
          if git diff --quiet; then
            echo "No changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/java/com/samaheim/SamaheimCaveGame.java
          git commit -m "Integrate deterministic resource regrowth"
          git push origin HEAD:work/resource-regrowth
