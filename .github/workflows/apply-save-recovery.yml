name: Apply Save Recovery

on:
  push:
    branches:
      - work/save-recovery
    paths:
      - tools/apply-save-recovery.py
      - .github/workflows/apply-save-recovery.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/save-recovery
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply save recovery integration
        run: python tools/apply-save-recovery.py
      - name: Commit integration
        run: |
          if git diff --quiet; then
            echo "No changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/java/com/samaheim/SamaheimCaveGame.java
          git commit -m "Integrate atomic save recovery"
          git push origin HEAD:work/save-recovery
