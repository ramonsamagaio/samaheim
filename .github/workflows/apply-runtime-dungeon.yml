name: Apply Runtime Dungeon

on:
  push:
    branches:
      - work/first-runtime-dungeon
    paths:
      - tools/apply-runtime-dungeon.py
      - .github/workflows/apply-runtime-dungeon.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/first-runtime-dungeon
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply runtime dungeon integration
        run: python tools/apply-runtime-dungeon.py
      - name: Commit integration
        run: |
          if git diff --quiet; then
            echo "No changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/java/com/samaheim/SamaheimCaveGame.java
          git commit -m "Integrate playable runtime dungeon"
          git push origin HEAD:work/first-runtime-dungeon
